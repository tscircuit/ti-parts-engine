{
  "name": "@tscircuit/ti-parts-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup index.ts --format esm --dts --sourcemap",
    "test": "bun test",
    "verify:lm358-bridge": "bun run scripts/verify-lm358-bridge.ts",
    "test:lm358-bridge": "bun run verify:lm358-bridge",
    "typecheck": "bunx tsc --noEmit",
    "format": "biome format . --write",
    "format:check": "biome format ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "@types/bun": "^1.2.22",
    "jszip": "^3.10.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
